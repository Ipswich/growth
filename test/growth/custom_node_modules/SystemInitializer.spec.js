const chai = require('chai').assert
const OutputState = require('../../../custom_node_modules/state_modules/OutputState.js')
const SensorState = require('../../../custom_node_modules/state_modules/SensorState.js')
const SystemInitializer = require('../../../custom_node_modules/SystemInitializer.js')

describe('SystemInitalizer.js Tests', () => {
  describe("DS18B20Initializer()", () => {
    let state = {}
    before(async () => {
      state.sensorState = await new SensorState();
    })
    it('should return an object with a count and pin', () => {
      let val = {count: 2, pin: 21}
      chai.deepEqual(val, SystemInitializer.DS18B20Initializer(state))
    })
  })

  describe("createBoardObject()", () => {
    it('should return a board object', () => {
      let config = {board:'test'}
      chai.notEqual(null, SystemInitializer.createBoardObject(config))
    })
  })
  
  describe("DS18B20AddressFilter()", () => {
    it('should add addresses from the board to DS18B20_Array, then emit \'booted\'', () => {
    })
  })
})